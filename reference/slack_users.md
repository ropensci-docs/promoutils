# Fetch a list of Slack users

Fetch a list of Slack users

## Usage

``` r
slack_users()
```

## Value

Data frame of Slack users including names and ids

## Examples

``` r
if (FALSE) { # interactive()
u <- slack_users()
}
```
